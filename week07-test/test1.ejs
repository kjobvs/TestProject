<% Object.keys(albumMap).forEach(key=>{ %>
  <% if(albumMap[key]){ %>
 <% for(let i of albumMap[key]){ %>
<img  src="/users/img/<%=  i%>" >
<% } %>
<% } %>
<% }) %>


albumMap ={"default": [ ]} //这是数据格式，对象里面很多个数组构成